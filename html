<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FORTUNE EARNERS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brandDarkGreen: '#042416', // Deep emerald background
                        brandGreenCard: '#0B3B24', // Inner panels
                        brandLightGreen: '#105936',
                        brandLime: '#DDF247',      // Bright accent
                        brandBrightGreen: '#22C55E'
                    }
                }
            }
        }
    </script>
    <style>
        .view-section { display: none; }
        .view-section.active { display: block; }
    </style>
</head>
<body class="bg-brandDarkGreen text-gray-100 font-sans antialiased pb-24">

    <div id="loading-screen" class="fixed inset-0 z-[100] bg-brandDarkGreen flex flex-col items-center justify-center transition-all duration-300">
        <div class="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-brandLime mb-4"></div>
        <p class="text-sm font-semibold tracking-wider text-gray-300 uppercase tracking-widest">Loading Gateway...</p>
    </div>

    <div id="auth-container" class="min-h-screen flex items-center justify-center px-4 py-12">
        <div id="auth-signin" class="view-section active w-full max-w-md bg-brandGreenCard border border-emerald-900 rounded-3xl p-6 space-y-6 shadow-xl">
            <div class="text-center">
                <h2 class="text-2xl font-black tracking-wider text-brandLime">FORTUNE EARNERS</h2>
                <p class="text-xs text-emerald-300/70 mt-1">Sign in to manage your workspace</p>
            </div>
            <form id="signin-form" onsubmit="event.preventDefault(); handleMockAuth();" class="space-y-4">
                <div>
                    <label class="text-xs font-bold text-emerald-300 uppercase tracking-wide">Username or Email</label>
                    <input type="text" id="signin-user" value="CoachDaniel" required class="w-full mt-1.5 bg-brandDarkGreen border border-emerald-800 focus:border-brandLime p-3.5 rounded-xl text-sm text-white focus:outline-none transition">
                </div>
                <div>
                    <label class="text-xs font-bold text-emerald-300 uppercase tracking-wide">Password</label>
                    <input type="password" value="password" required class="w-full mt-1.5 bg-brandDarkGreen border border-emerald-800 focus:border-brandLime p-3.5 rounded-xl text-sm text-white focus:outline-none transition">
                </div>
                <button type="submit" class="w-full bg-brandLime text-brandDarkGreen font-extrabold py-3.5 rounded-xl shadow-md text-sm uppercase tracking-wider">
                    Sign In Account
                </button>
            </form>
        </div>
    </div>

    <div id="app-container" class="hidden">
        
        <header class="sticky top-0 z-50 bg-brandDarkGreen/90 backdrop-blur-md border-b border-emerald-950 px-4 py-3 flex items-center justify-between">
            <div class="flex items-center space-x-3">
                <button class="w-9 h-9 rounded-xl bg-emerald-900/40 border border-emerald-800 flex items-center justify-center text-gray-300 text-lg">
                    <i class="fas fa-th-large"></i>
                </button>
            </div>
            <div class="flex items-center space-x-3">
                <div class="flex items-center bg-brandGreenCard border border-emerald-800 px-2.5 py-1 rounded-full text-xs space-x-1.5">
                    <span class="text-sm">🇳🇬</span>
                    <span class="font-bold text-emerald-200">NG</span>
                </div>
                <div class="w-8 h-8 rounded-full border border-brandLime bg-brandLime text-brandDarkGreen flex items-center justify-center text-[10px] font-black uppercase">
                    FE
                </div>
            </div>
        </header>

        <main class="max-w-md mx-auto p-4 space-y-4">

            <div id="view-dashboard" class="view-section active space-y-4">
                
                <div class="bg-brandGreenCard border border-emerald-900/60 rounded-3xl p-5 space-y-5 shadow-xl">
                    <div class="flex justify-between items-center">
                        <div class="flex items-center space-x-3">
                            <div class="w-12 h-12 rounded-full bg-emerald-800/60 border border-emerald-700 flex items-center justify-center text-sm font-black text-brandLime uppercase">
                                CO
                            </div>
                            <div>
                                <p class="text-xs text-emerald-400/80">Welcome Back</p>
                                <h2 id="dyn-user" class="text-base font-black text-white">CoachDaniel</h2>
                            </div>
                        </div>
                        <span class="bg-brandLime text-brandDarkGreen font-black text-[10px] px-3 py-1.5 rounded-full uppercase tracking-wider shadow-sm">
                            NEWBIE PLAN
                        </span>
                    </div>

                    <div class="bg-brandLime rounded-3xl p-5 text-brandDarkGreen flex justify-between items-center relative overflow-hidden shadow-lg">
                        <div class="absolute -right-4 -bottom-4 w-20 h-20 rounded-full bg-emerald-900/5 border-[8px] border-emerald-900/10 pointer-events-none"></div>
                        <div>
                            <span class="text-xs font-semibold opacity-75 block tracking-wide">Account Balance</span>
                            <span class="text-2xl font-black mt-0.5 block">₦<span id="dyn-total-bal">12,000.00</span></span>
                        </div>
                        <button onclick="switchMainView('withdraw')" class="bg-brandDarkGreen hover:bg-brandDarkGreen/95 text-white font-extrabold text-xs px-5 py-2.5 rounded-xl shadow-md transition">
                            Withdraw
                        </button>
                    </div>

                    <div class="bg-emerald-900/30 border border-emerald-800/40 p-3 rounded-2xl space-y-3">
                        <div class="grid grid-cols-2 gap-3">
                            <div class="bg-brandDarkGreen border border-emerald-900 p-3 rounded-xl flex items-center space-x-3">
                                <div class="w-8 h-8 rounded-lg bg-emerald-900 flex items-center justify-center text-brandLime text-xs"><i class="fas fa-users"></i></div>
                                <div>
                                    <span class="text-sm font-black text-brandLime block">₦<span id="dyn-aff-bal">800.00</span></span>
                                    <span class="text-[9px] text-gray-400 uppercase font-bold block">Affiliate</span>
                                </div>
                            </div>
                            <div class="bg-brandDarkGreen border border-emerald-900 p-3 rounded-xl flex items-center space-x-3">
                                <div class="w-8 h-8 rounded-lg bg-emerald-900 flex items-center justify-center text-brandLime text-xs"><i class="fas fa-briefcase"></i></div>
                                <div>
                                    <span class="text-sm font-black text-brandLime block">₦<span id="dyn-task-bal">11,200.00</span></span>
                                    <span class="text-[9px] text-gray-400 uppercase font-bold block">Tasks Earnings</span>
                                </div>
                            </div>
                        </div>

                        <div class="bg-brandDarkGreen border border-emerald-900 rounded-xl p-2.5 flex items-center justify-between">
                            <div class="flex items-center space-x-2 overflow-hidden w-full mr-2">
                                <div class="text-brandLime text-xs"><i class="fas fa-wallet"></i></div>
                                <input id="dyn-ref-url" type="text" class="bg-transparent text-[11px] text-brandLime w-full focus:outline-none select-all font-mono" readonly value="">
                            </div>
                            <button onclick="copyReferralLink()" class="w-7 h-7 rounded-lg bg-emerald-900 hover:bg-emerald-800 text-brandLime flex items-center justify-center transition flex-shrink-0">
                                <i class="far fa-copy text-xs"></i>
                            </button>
                        </div>
                    </div>
                </div>

                <div class="bg-brandGreenCard border border-emerald-900 rounded-3xl p-5 space-y-4 shadow-xl">
                    <div class="flex justify-between items-center">
                        <h3 class="text-sm font-bold text-white tracking-wide">Weekly Top Affiliates</h3>
                        <button class="bg-emerald-500/20 text-brandBrightGreen text-[11px] font-bold px-3 py-1 rounded-full flex items-center space-x-1">
                            <span>View All</span> <i class="fas fa-chevron-right text-[8px] ml-1"></i>
                        </button>
                    </div>
                    <p class="text-xs text-gray-400 -mt-2">This week winners</p>
                    
                    <div class="grid grid-cols-3 gap-2 pt-2 text-center">
                        <div class="flex flex-col items-center space-y-2">
                            <span class="bg-orange-600 text-white font-bold text-[9px] px-2 py-0.5 rounded-full">₦10,000</span>
                            <div class="w-14 h-14 rounded-full border-2 border-emerald-600 overflow-hidden bg-gray-700">
                                <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=150&q=80" class="w-full h-full object-cover">
                            </div>
                        </div>
                        <div class="flex flex-col items-center space-y-2">
                            <span class="bg-orange-600 text-white font-bold text-[9px] px-2 py-0.5 rounded-full">₦5,000</span>
                            <div class="w-14 h-14 rounded-full border-2 border-emerald-600 overflow-hidden bg-gray-700">
                                <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=150&q=80" class="w-full h-full object-cover">
                            </div>
                        </div>
                        <div class="flex flex-col items-center space-y-2">
                            <span class="bg-orange-600 text-white font-bold text-[9px] px-2 py-0.5 rounded-full">₦3,000</span>
                            <div class="w-14 h-14 rounded-full border-2 border-emerald-600 flex items-center justify-center bg-brandDarkGreen text-gray-400 text-xl">
                                <i class="fas fa-user-circle"></i>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="bg-brandGreenCard border border-emerald-900 rounded-2xl p-4 space-y-2">
                    <button onclick="runDailyTask()" class="w-full bg-brandDarkGreen border border-emerald-900 py-3.5 px-4 rounded-xl flex items-center justify-between text-sm hover:border-brandLime transition">
                        <div class="flex items-center space-x-3">
                            <i class="fas fa-check-double text-brandLime text-base"></i>
                            <span class="font-medium text-gray-200">Perform Daily Micro-Tasks</span>
                        </div>
                        <span id="task-count-badge" class="text-xs bg-emerald-900 text-brandLime px-2.5 py-1 rounded-md">5 Left</span>
                    </button>
                </div>
            </div>

            <div id="view-ads" class="view-section space-y-4">
                <div class="bg-brandGreenCard border border-emerald-900 rounded-3xl p-6 text-center space-y-4 shadow-xl">
                    <div class="w-14 h-14 mx-auto rounded-2xl bg-emerald-900 flex items-center justify-center text-brandLime text-xl"><i class="fas fa-rocket"></i></div>
                    <h2 class="font-black text-lg text-white">Promote Your Business</h2>
                    <p class="text-xs text-gray-400 max-w-xs mx-auto leading-relaxed">Reach thousands of active online members instantly.</p>
                </div>
            </div>

            <div id="view-withdraw" class="view-section space-y-4">
                <div class="bg-brandGreenCard border border-emerald-900 rounded-3xl p-5 space-y-4 shadow-xl">
                    <h2 class="text-sm font-black text-white uppercase tracking-wider flex items-center space-x-2">
                        <i class="fas fa-wallet text-brandLime"></i><span>Secure Payout System</span>
                    </h2>
                    <div class="bg-emerald-900/40 p-4 rounded-xl border border-emerald-800 text-xs text-emerald-200">
                        Withdrawals are processed instantly to your registered bank account structure.
                    </div>
                    <button onclick="alert('Withdrawal request logged successfully!')" class="w-full bg-brandLime text-brandDarkGreen font-bold py-3 rounded-xl text-xs uppercase">Process Account Cashout</button>
                </div>
            </div>

            <div id="view-referrals" class="view-section space-y-4">
                <div class="bg-brandGreenCard border border-emerald-900 rounded-3xl p-5 space-y-4 shadow-xl">
                    <h2 class="text-sm font-black text-white uppercase tracking-wider"><i class="fas fa-users text-brandLime mr-2"></i>My Team Downlines</h2>
                    <div class="grid grid-cols-2 gap-3 text-center text-xs">
                        <div class="bg-brandDarkGreen p-3 rounded-xl"><p class="text-gray-400">Total Invited</p><p class="text-lg font-black text-white mt-0.5">14</p></div>
                        <div class="bg-brandDarkGreen p-3 rounded-xl"><p class="text-brandLime">Active Plans</p><p class="text-lg font-black text-brandLime mt-0.5">8</p></div>
                    </div>
                </div>
            </div>

            <div id="view-profile" class="view-section space-y-4">
                <div class="bg-brandGreenCard border border-emerald-900 rounded-3xl p-5 space-y-4 shadow-xl">
                    <div class="text-center pb-4 border-b border-emerald-900">
                        <div class="w-16 h-16 mx-auto rounded-full bg-brandDarkGreen border-2 border-brandLime flex items-center justify-center text-lg font-black text-brandLime mb-2">FE</div>
                        <h3 class="font-bold text-base text-white">Coach Daniel</h3>
                        <p class="text-xs text-brandLime opacity-80">@coachdaniel</p>
                    </div>
                    <button onclick="handleLogout()" class="w-full bg-red-600/20 text-red-200 border border-red-900 font-bold py-3 rounded-xl text-xs uppercase">Sign Out Session</button>
                </div>
            </div>

        </main>

        <footer class="fixed bottom-0 left-0 right-0 bg-brandGreenCard/95 backdrop-blur-md border-t border-emerald-950 px-2 py-2 z-50 shadow-2xl">
            <div class="max-w-md mx-auto grid grid-cols-5 gap-0.5 text-center">
                <button onclick="switchMainView('dashboard', this)" class="tab-btn text-brandLime flex flex-col items-center justify-center py-2 rounded-xl transition">
                    <i class="fas fa-tasks text-base"></i><span class="text-[9px] font-bold mt-1 uppercase">Tasks</span>
                </button>
                <button onclick="switchMainView('ads', this)" class="tab-btn text-gray-400 flex flex-col items-center justify-center py-2 rounded-xl transition">
                    <i class="fas fa-bullhorn text-base"></i><span class="text-[9px] font-bold mt-1 uppercase">Ads</span>
                </button>
                <button onclick="switchMainView('dashboard', this)" class="tab-btn text-gray-400 flex flex-col items-center justify-center py-2 rounded-xl transition">
                    <div class="w-10 h-10 rounded-full bg-brandDarkGreen text-brandLime border border-emerald-800 flex items-center justify-center shadow-lg transform -translate-y-2"><i class="fas fa-home text-base"></i></div>
                </button>
                <button onclick="switchMainView('referrals', this)" class="tab-btn text-gray-400 flex flex-col items-center justify-center py-2 rounded-xl transition">
                    <i class="fas fa-user-friends text-base"></i><span class="text-[9px] font-bold mt-1 uppercase">Refer</span>
                </button>
                <button onclick="switchMainView('profile', this)" class="tab-btn text-gray-400 flex flex-col items-center justify-center py-2 rounded-xl transition">
                    <i class="fas fa-user text-base"></i><span class="text-[9px] font-bold mt-1 uppercase">Profile</span>
                </button>
            </div>
        </footer>
    </div>

    <script>
        // App Core State (Loads from Browser Storage if present)
        let state = {
            username: localStorage.getItem('fe_user') || 'CoachDaniel',
            taskWallet: parseFloat(localStorage.getItem('fe_task')) || 11200.00,
            affiliateWallet: parseFloat(localStorage.getItem('fe_aff')) || 800.00,
            tasksLeft: parseInt(localStorage.getItem('fe_left')) || 5
        };

        // Initialize App UI data directly on screen mount
        window.addEventListener('DOMContentLoaded', () => {
            // Speed loader dismissal: hides in exactly 300ms automatically
            setTimeout(() => {
                document.getElementById('loading-screen').classList.add('opacity-0');
                setTimeout(() => {
                    document.getElementById('loading-screen').classList.add('hidden');
                }, 150);
            }, 300);
            
            updateUIState();
        });

        function updateUIState() {
            document.getElementById('dyn-user').innerText = state.username;
            document.getElementById('dyn-task-bal').innerText = state.taskWallet.toLocaleString('en-US', { minimumFractionDigits: 2 });
            document.getElementById('dyn-aff-bal').innerText = state.affiliateWallet.toLocaleString('en-US', { minimumFractionDigits: 2 });
            
            let total = state.taskWallet + state.affiliateWallet;
            document.getElementById('dyn-total-bal').innerText = total.toLocaleString('en-US', { minimumFractionDigits: 2 });
            document.getElementById('task-count-badge').innerText = `${state.tasksLeft} Left`;
            
            // Sets a dynamic window relative referral link instead of hardcoded external domains
            document.getElementById('dyn-ref-url').value = `${window.location.origin}/signup?ref=${state.username}`;
        }

        function handleMockAuth() {
            state.username = document.getElementById('signin-user').value || 'CoachDaniel';
            localStorage.setItem('fe_user', state.username);
            
            document.getElementById('auth-container').classList.add('hidden');
            document.getElementById('app-container').classList.remove('hidden');
            updateUIState();
            switchMainView('dashboard');
        }

        function runDailyTask() {
            if(state.tasksLeft <= 0) {
                alert("Daily limit reached for today!");
                return;
            }
            state.tasksLeft -= 1;
            state.taskWallet += 200; 
            
            localStorage.setItem('fe_left', state.tasksLeft);
            localStorage.setItem('fe_task', state.taskWallet);
            
            alert("Task performance executed! ₦200 credited.");
            updateUIState();
        }

        function switchMainView(viewId, targetElement = null) {
            document.querySelectorAll('#app-container .view-section').forEach(section => section.classList.remove('active'));
            const targetView = document.getElementById(`view-${viewId}`);
            if(targetView) targetView.classList.add('active');

            if (targetElement) {
                document.querySelectorAll('.tab-btn').forEach(btn => {
                    btn.classList.remove('text-brandLime');
                    btn.classList.add('text-gray-400');
                });
                targetElement.classList.remove('text-gray-400');
                targetElement.classList.add('text-brandLime');
            }
        }

        function copyReferralLink() {
            const linkInput = document.getElementById('dyn-ref-url');
            linkInput.select();
            navigator.clipboard.writeText(linkInput.value);
            alert("Referral link copied to device clipboard!");
        }

        function handleLogout() {
            document.getElementById('app-container').classList.add('hidden');
            document.getElementById('auth-container').classList.remove('hidden');
        }
    </script>
</body>
</html>!